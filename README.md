```bash
# clone the repo
git clone https://github.com/buttermiilk/what-even.git
# grab packages
sudo pacman -S hyprland kitty neovim tmux fish waybar swaybg swayidle kanata ripgrep niri
# symlinks
bash $HOME/dotfiles/scripts/setup-config.sh
```